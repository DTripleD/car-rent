export const getCars = (state) => state.cars.items;

export const getFavorite = (store) => store.favorite.data;

export const getLoading = (state) => state.cars.isLoading;

export const store = (store) => store;
